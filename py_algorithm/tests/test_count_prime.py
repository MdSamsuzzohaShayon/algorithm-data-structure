# tests/test_count_prime.py

import sys
import os

# Add the parent directory of the project to sys.path
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

# Now you can import the module directly
# from aa_count_prime_num_ex import is_prime, is_prime_optimized
#
#
# def test_naive_prime():
#     assert is_prime(29) == True, "29 should be identified as a prime number"
#     assert is_prime(1) == False, "1 should not be identified as a prime number"
#     assert is_prime(18) == False, "18 should not be identified as a prime number"
#
#
# def test_optimized_prime():
#     assert is_prime_optimized(29) == True, "29 should be identified as a prime number"
#     assert is_prime_optimized(1) == False, "1 should not be identified as a prime number"
#     assert is_prime_optimized(18) == False, "18 should not be identified as a prime number"
